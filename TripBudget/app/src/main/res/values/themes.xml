<resources>
    <!-- Base application theme. The real theming lives in Compose (ui/theme) —
         this XML theme only needs to exist so the manifest / launch splash
         has something to point at before Compose takes over. -->
    <style name="Theme.TripBudget" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">#0E5B54</item>
        <item name="android:windowBackground">#F6F6F3</item>
    </style>
</resources>
